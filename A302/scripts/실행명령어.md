자기 디렉토리에 맞게 변경

```powershell
cd C:\Users\SSAFY\Desktop\2div\S14P21A302\A302
```

서버 스택 실행(로비 + Dedicated 서버만 실행, 에디터 클라이언트는 직접 Standalone 실행):

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-local-test-stack.ps1
```

Dedicated 서버만 실행(로비 서버는 이미 켜져 있을 때):

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-local-test-stack.ps1 -SkipLobby
```

로컬 테스트 프로세스 종료:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\stop-local-test-stack.ps1
```

테스트 흐름:
- Editor Standalone 첫 맵: `testLevel`
- Dedicated 서버 기본 맵: `MyMap` (한 DS 다중 룸: ServerTravel 없이 같은 월드 유지)
- 방 생성 UI에서 시작: 같은 인스턴스에서 RoomCode 기반으로 상태 전환

서버 맵의 클라 전용 레퍼런스 정리(에디터 Python):

```powershell
C:\UnrealEngine\Engine\Binaries\Win64\UnrealEditor-Cmd.exe `
  C:\Users\SSAFY\Desktop\2div\S14P21A302\A302\A302.uproject `
  -ExecutePythonScript=C:/Users/SSAFY/Desktop/2div/S14P21A302/A302/scripts/sanitize_server_map_assets.py `
  -unattended -nop4 -NullRHI -NoSplash -log
```

의존성 감사만 수행(읽기 전용):

```powershell
C:\UnrealEngine\Engine\Binaries\Win64\UnrealEditor-Cmd.exe `
  C:\Users\SSAFY\Desktop\2div\S14P21A302\A302\A302.uproject `
  -ExecutePythonScript=C:/Users/SSAFY/Desktop/2div/S14P21A302/A302/scripts/dump_server_map_dependencies.py `
  -unattended -nop4 -NullRHI -NoSplash -log
```

에디터 실행이 어려운 환경에서 빠른 문자열 감사(맵/핵심 에셋 내 클라 참조 확인):

```powershell
python .\scripts\audit_server_asset_strings.py
```
